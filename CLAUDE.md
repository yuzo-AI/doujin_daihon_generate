# プロジェクト: doujin_daihon_generate

YouTubeショート用「寸止め台本」の自動生成プロジェクト。

## ディレクトリ構成
- `要件定義.txt` — 台本生成のルール・フロー・Few-Shot例（システムプロンプト本体）
- `Input/Image/` — 今回処理する漫画画像
- `Input/synopsis.txt` — あらすじテキスト
- `Input/_ocr/ocr_text.txt` — OCR抽出済みテキスト（mokuro出力。あれば優先使用）
- `Output/YYYYMMDD/` — 生成された台本の保存先
- `参考チャンネル1〜3/` — 学習用リファレンス（編集禁止）

## 「Run」コマンド（重要）

ユーザーが **`Run`** とだけ入力したら、以下を自動実行すること。確認は不要、即実行。

### 実行手順

1. **要件定義の読込**: `要件定義.txt` を読み、Step1〜Step4のフローを頭に入れる。
2. **素材の読込**（可能な限り並列で `Read` すること）:
   - `Input/synopsis.txt` を読む
   - `Input/_ocr/ocr_text.txt` が存在すれば読む（各ページのセリフ・地の文の正確な内容。ストーリー時系列把握に優先使用）
   - `Input/Image/` 配下の全画像を読む（コマ構成・表情・ページレイアウトの視覚確認用）
   - ※ OCRテキストがある場合はセリフ内容はそちらを優先し、画像はコマ位置・構図・感情の確認に使う
3. **Step1 ジャンル判定**: A(日常ラブコメ)／B(わからせ)／C(能力者ツイスト)／D(NTR)／E(ハーレム) のいずれかに分類。
4. **Step2 テンプレ選択**: 判定ジャンルに応じてオープニング型・寸止め型・ナレ基調を決定。
5. **Step3 構成設計**: 21〜25カットに感情曲線（お？→は？→え！？→うわ…→寸止め）を配置。
6. **Step4 生成＋自己チェック**: 要件定義の自己チェック項目を全てクリアしていること。特に以下を必ず確認：
   - テキストだけ読んでストーリーが追えるか（「なぜそうなったか」が文脈で分かるか）
   - 「ト書き的背景説明」（実は〜のだが／どうやら〜らしい／こうして〜）が適切に使われているか
   - 行動やセリフの直前にその動機・状況説明が入っているか
7. **出力保存（必ず2ファイルをセットで出力）**:
   - **① 台本本体**: `Output/{今日の日付YYYYMMDD}/台本.txt`
     - 既にファイルがある場合は `台本_2.txt`, `台本_3.txt` とナンバリング
     - 冒頭に `# ジャンル: X / OP: Y / 寸止め: Z` のメタ行を付与
     - 本文は `【画像指示：〇〇】` と `[話者タグ] テロップ本文` を交互に繰り返すプレーンテキスト
   - **② Voice.csv（Vrew音声合成用）**: `Output/{今日の日付YYYYMMDD}/台本_Voice.csv`
     - 台本.txtと連番を揃える（`台本_2.txt` なら `台本_2_Voice.csv`）
     - 形式: `話者名,テロップ本文` の2カラム。ヘッダー行なし、BOM付きUTF-8で保存
     - `【画像指示：〇〇】` 行は**含めない**（ナレ/セリフのみ）
     - CTA（固定エンディング）も1行として含める
     - 話者タグ→話者名の変換は下記「話者マッピング」に従う
8. **完了報告**: 保存パス（2ファイル）、カット数、判定ジャンル、選択した各型を1〜2文で報告。

### 話者タグ
- `[ナレ]` 三人称ナレーション
- `[男主]` 男性主人公
- `[女主]` 女性主人公
- `[男脇]` 男性脇役
- `[女脇]` 女性脇役

### 話者マッピング（Voice.csv変換用・Vrew/VOICEVOX想定）
| タグ | 話者名（CSV出力値） |
|---|---|
| `[ナレ]` | 青山龍星 |
| `[男主]` | 白上虎太郎 |
| `[男脇]` | 青山龍星 |
| `[女主]` | もち子さん |
| `[女脇]` | 春日部つむぎ |

※この対応表を変更したい場合はCLAUDE.mdのこの表を編集すれば反映される。

### 注意
- AIの挨拶・「承知しました」等の前置きは一切出力しない
- 要件定義のNGワード変換は必ず適用（セーフティフィルター回避）
- 同じ入力で複数回 `Run` された場合、ファイル名をナンバリングして上書きしない

## その他のコマンド

- `Run 差し替え` — 既存 `Output/{今日の日付}/台本.txt` + `台本_Voice.csv` を同じ入力で書き直して上書き
- `Run CSV` — 既存の最新台本からVoice.csvだけを再生成（台本本体は触らない）
